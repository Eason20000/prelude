# Prelude

A MIDI file player built with GTK4 and libadwaita, respecting the GNOME Human Interface Guidelines.

## Usage

```bash
nix run .
```

Open a MIDI file via the file dialog, then control playback with the on-screen
buttons or the space bar.

## Development

```bash
nix develop
cargo build
```
